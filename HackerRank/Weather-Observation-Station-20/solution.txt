/*
Enter your query here.
*/

WITH ranked AS (
    SELECT
        LAT_N,
        ROW_NUMBER() OVER (ORDER BY LAT_N) AS rn,
        COUNT(*) OVER () AS cnt
    FROM STATION
)
SELECT ROUND(AVG(LAT_N), 4)
FROM ranked
WHERE rn IN (
    FLOOR((cnt + 1) / 2),
    FLOOR((cnt + 2) / 2)
);
